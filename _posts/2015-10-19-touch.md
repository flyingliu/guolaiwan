---
layout: default
title: 微信等移动端浏览器禁止页面上下拉动
tags: [git]
---

微信等内置浏览器在页面上下拉动时有默认事件，下面代码可以禁止。

    handler = function (e) {
      e.preventDefault();
    };
    document.addEventListener("touchmove", handler, false);